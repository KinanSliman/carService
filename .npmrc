# eslint-config-next pulls its plugins in as transitive deps. pnpm's strict
# node_modules keeps them out of the root, where ESLint's resolver looks for
# them, so they are hoisted explicitly rather than listed as fake direct
# dependencies of this project.
public-hoist-pattern[]=*eslint*
public-hoist-pattern[]=@typescript-eslint/*
